# bash-functions
Bash function to be reused for providing automated backstage plugin installation possibilities like adding imports or routes to specific Backstage source files.
This repository is expected to be used as a submodule in Backstage plugin repository and **functions.sh** to be sourced using `source ./bash-functions/functions.sh`.
